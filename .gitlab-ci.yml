image: registry.gitlab.com/fdroid/docker-executable-fdroidserver:master

pages:
  stage: deploy
  script:
    - mkdir .public
    - cp -r * .public
    - mv .public public
  artifacts:
    paths:
      - public
  only:
    - master
